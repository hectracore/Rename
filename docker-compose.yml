version: '3.8'

services:
  xtv-mediastudio:
    build: .
    container_name: xtv-mediastudio
    restart: unless-stopped
    env_file:
      - .env
    # Optional: If you want to persist the local pyrogram sessions outside the container
    # volumes:
    #   - ./:/app
